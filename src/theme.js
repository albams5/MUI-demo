import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#1760a5',
            light: "skyblue",
        },
        secondary: {
            main: '#673ab7'
        },
        otherColor: {
            main: "#B22222"
        }
    }
})